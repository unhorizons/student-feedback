<div class="bq-note-item pos-rel">
    <div class="bq-note-text">
        <p>{{ data.content }}</p>
    </div>
    <div class="bq-note-meta">
        <span class="bq-note-added">
            Ajouté le <span class="date">{{ data.createdAt | date('d M, Y') }}</span> à <span class="time">{{ data.createdAt | date('H:i') }}</span>
        </span>
        <span class="bq-note-sep sep">|</span>
        <span class="bq-note-by">par <span>{{ data.owner.email }}</span></span>

        <a class="link link-sm" href="{{ path('administration_feedback_feedback_response_edit', {feedback: feedback, id: data.id}) }}">Modifier</a>
    </div>
</div>
